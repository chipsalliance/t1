[package]
name = "online_vcs"
edition = "2021"
version.workspace = true

[lib]
crate-type = ["staticlib"]
name = "dpi"

[dependencies]
online_dpi = { path = "../online_dpi" }
svdpi = { workspace = true, features = ["vpi"] }

[features]
trace = ["online_dpi/trace"]
