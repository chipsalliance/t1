pipeline {
  agent any
  options {
    skipDefaultCheckout(true)
  }
  stages {
    stage('Build') {
      steps {
        cleanWs()
        checkout scm
        sh 'make init'
        sh 'make patch'
        sh 'env -i nix develop -c make test'
      }
    }
  }
}
