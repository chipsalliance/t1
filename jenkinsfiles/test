pipeline {
  agent any
  options {
    skipDefaultCheckout(true)
  }
  stages {
    stage('Tests') {
      steps {
        cleanWs()
        checkout scm
        sh 'make init'
        sh 'make patch'
        sh 'make ci'
      }
    }
  }
}
